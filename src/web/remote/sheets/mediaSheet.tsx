import { Sheet } from '@remote/types/types'
import { FullmediaTile } from '@tiles/FullmediaTile'
import { TranslateTile } from '@tiles/TranslateTile'

export const mediaSheet: Sheet = {
    '50': [<FullmediaTile />],
    '51': [, , <TranslateTile />]
}
